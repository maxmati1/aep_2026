spring:
  application:
    name: cidadao-ativo
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  datasource:
    url: jdbc:h2:mem:cidadao-ativo
    driverClassName: org.h2.Driver
server:
  port: 8080
  servlet:
    context-path: /api